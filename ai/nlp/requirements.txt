# ═══════════════════════════════════════════════════════════════════
# requirements.txt — B4 NLP FastAPI Service (LLM Edition)
# Smart City Women's Safety System — TigerGraph Hackathon
# ═══════════════════════════════════════════════════════════════════
#
# Install:   pip install -r requirements.txt
#
# LLM mode: Set NVIDIA_API_KEY env var to use NVIDIA NIM (Llama-3.3-70b).
# Without key: service runs in mock mode with keyword-based defaults.
# ═══════════════════════════════════════════════════════════════════

# ── Web framework ────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# ── ML / text processing (lightweight — no HuggingFace/spaCy) ───
scikit-learn>=1.4.0
numpy>=1.26.0

# ── Utilities ────────────────────────────────────────────────────
python-multipart>=0.0.9       # needed for FastAPI form/file uploads (future use)

# ── Dev / testing (optional — comment out for minimal production image) ──
# httpx>=0.27.0               # async test client for pytest
# pytest>=8.1.0
# pytest-asyncio>=0.23.0
